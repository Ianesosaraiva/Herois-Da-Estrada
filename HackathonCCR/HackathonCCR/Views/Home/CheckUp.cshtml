
@{
    ViewBag.Title = "Check-Up";
    string message = ViewBag.message as string;
}

<div class="content">
    <div class="container">
        <div class="row">
            <div class="col-lg-10 col-md-8 col-sm-12 ml-auto mr-auto">
                <div class="card">
                    <div class="container my-4 mt-0">
                        <div class="card-header text-center">
                        </div>

                        <h5 class="text-center" style="font-weight: bold">Vamos fazer um check-up?</h5>
                        @if (message != null)
                        {
                            <h5 class="card-description text-danger text-center">@message</h5>
                        }

                        <div class="row form-group text-center">
                            <div class="col-sm-4 mt-2">
                                <a href="@Url.Action("Resultado", "Home")" class="btn btn-primary"><b>Monitorar Pressão Arterial</b></a>
                            </div>
                            <div class="col-sm-4 mt-2">
                                <a href="@Url.Action("Resultado", "Home")" class="btn btn-primary"><b>Monitorar Batimento Cardíaco</b></a>
                            </div>
                            <div class="col-sm-4 mt-2">
                                <a href="@Url.Action("Resultado", "Home")" class="btn btn-primary"><b>Monitorar Gaseometria</b></a>
                            </div>
                        </div>
                        @*<div class="text-center">
                            <input type="submit" value="Salvar" class="btn btn-success mt-4" />
                        </div>*@

                        @*@using (Html.BeginForm())
                            {
                                @Html.AntiForgeryToken()

                                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                                <div class="row form-group">
                                    @foreach (var item in sensacoes)
                                    {
                                        <div class="col-sm-4 mt-2">
                                            <p style="font-weight:bold">
                                                <input class="form-check-inline" type="checkbox" name=mybox[] value="@item.nome" />
                                                @item.nome
                                            </p>
                                        </div>
                                    }
                                </div>
                                <div class="text-center">
                                    <a href="@Url.Action("CriarEquipamentoEspecificacoes", "EquipamentoEspecificacao", new { EquipamentoId = EquipamentoId})" class="btn btn-primary">Voltar</a>
                                    <input type="submit" id="criar" value="Salvar" class="btn btn-primary" />
                                </div>
                            }*@
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {

    @Scripts.Render("~/bundles/jqueryval")
}
